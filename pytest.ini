[pytest]
# Parallel test runs by default (suite is CPU-bound and parallel-safe).
# Opt out for debugging (pdb, -s) with: pytest -n0 ...
addopts = -n auto
testpaths = test
markers =
    slow: marks tests as slow (deselect with '-m "not slow"')
